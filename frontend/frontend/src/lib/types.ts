export interface overlayData {
    // content: string,
    current_id: string,
    current_md: string,
    // current_text: string,
    // mEvent: MouseEvent,
    chart: Boolean,
    definition: Boolean
}